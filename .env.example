# SQLite database (relative to /prisma)
DATABASE_URL="file:./dev.db"

# Optional — enables AI-written style notes on generated outfits.
# Without it the app falls back to a local rule-based note writer.
ANTHROPIC_API_KEY=your_key_here
